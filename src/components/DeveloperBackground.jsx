import React from 'react';

// Background is now handled directly by section backgrounds in ProfileContent.css
// This component is kept for potential future use
const DeveloperBackground = () => null;

export default DeveloperBackground;
